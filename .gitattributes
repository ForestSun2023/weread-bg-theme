# ---------------------------------------------------------------------------
# 换行与二进制处理
# ---------------------------------------------------------------------------
# 仓库内统一存 LF。不做这一步的话，不同人的编辑器 / core.autocrlf 设置不同，
# 每次提交都会产出「整个文件都改了」的假 diff
#（首次提交时 git 已经就 README.md 的 LF→CRLF 警告过一次）。
* text=auto eol=lf

# 图片：一律不做任何转换
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary

# PowerShell 脚本在 Windows 上按 CRLF 检出（Windows PowerShell 5.1 习惯于此）。
#
# ⚠️ 这些文件必须保持 **UTF-8 BOM**：5.1 对无 BOM 的 .ps1 会按 ANSI 代码页解码，
#    中文注释会变成乱码并直接报语法错误（本项目踩过，交付检查一度整个跑不起来）。
#    git 不会替你增删 BOM，所以 BOM 是文件内容的一部分，交由编辑器/提交者保证。
*.ps1 text eol=crlf
