# 微信读书网页版 · 背景颜色主题 · 使用说明

对应文件：`weread-bg-theme.user.js`（**v3.1.0**，功能：**亮度 / 颜色 / 背景 / 全屏阅读**）

支持两种阅读模式：**纵向滚动** 和 **横向 / 双栏翻页**。

---

## 一、安装油猴（Tampermonkey）

脚本需要油猴这类用户脚本管理器才能运行。

1. 打开浏览器的扩展商店，搜索 **Tampermonkey**（油猴）
   - Edge：[Edge 加载项](https://microsoftedge.microsoft.com/addons/detail/tampermonkey/iikmkjmpaadaobahmlepeloendndfphd)
   - Chrome：[Chrome 应用商店](https://chromewebstore.google.com/detail/tampermonkey/dhdgffkkebhmkfjojejmpbldmpobfkfo)
2. 点「添加到浏览器 / 获取」→ 确认安装
3. 装好后浏览器右上角会出现油猴图标（可能被折叠在拼图菜单里，建议点「固定」）

---

## 二、安装脚本

### 方式 A · 复制粘贴（推荐，最稳）

1. 点浏览器右上角 **油猴图标** → **管理面板 / 添加新脚本**
2. 编辑器里会有一段默认模板代码，**全选删除**（Ctrl+A → Delete）
3. 用记事本打开 `weread-bg-theme.user.js`，**全选复制**，粘贴进编辑器
4. 按 **Ctrl + S** 保存
5. 看到「已安装 / 已启用」即可

### 方式 B · 直接导入文件

1. 油猴面板 → **实用工具** → **导入** → 选择 `weread-bg-theme.user.js`
2. 或把 `.user.js` 文件直接拖进浏览器窗口，油猴会弹出安装页 → 点「安装」

> ⚠️ 用 `file://` 打开本地文件的方式安装时，需要在扩展详情里打开
> **「允许访问文件网址」**，否则可能装不上。装不上就用方式 A。

---

## 三、使用

1. 打开任意一章阅读页：`https://weread.qq.com/web/reader/...`
   （脚本匹配 `https://weread.qq.com/web/*`，只有 `/web/reader/` 页面会生效）
2. **把鼠标移到页面中间** —— 右侧工具条平时是隐藏的，移动鼠标才会浮出来
3. 工具条从上到下是：目录、Ai 问书、笔记、双栏阅读、字号、深色 —— 然后是**本脚本的两个按钮，固定在所有原生按钮之后**：**☀（背景）**、**⛶（全屏阅读）**
4. 点 **☀** → 背景面板从工具条左侧滑出（也可以直接按快捷键 **`b`** 开关）。**这个按钮只在白天出现**
5. 要换白天/黑夜，**请用站点原生的「深色」按钮**（月亮图标，就在「字号」下面）——
   脚本**故意不自己造这个按钮**，原因见下面那条说明
6. 点 **⛶** → 全屏阅读，再点一次或按 `Esc` 退出，图标和提示文字会跟着切换
7. **白天**：3 种底色 × 5 种纹理 = 15 种组合。**黑夜**：整块交还官方原装外观，
   脚本一点都不改（连正文颜色都不碰），只保留「全屏」一个按钮

> **关于全屏（重要）**
>
> - **JS 无法触发浏览器级的 F11** —— 那是浏览器自身功能，网页脚本没有权限。
>   这个按钮全屏的是「整个页面」（`<html>`），视觉上与 F11 一致：浏览器界面全隐藏、
>   页面铺满屏幕，站点布局完全不受影响（只是视口变大，排版引擎照常重排）。
> - **脚本能识别 F11**：如果你用 F11 进的全屏，按钮会自动变成
>   「⛶ 退出全屏（按 F11）」；退出 F11 后自动变回「全屏阅读」。
> - **但按钮退不出 F11** —— JS 同样没有权限退出浏览器原生全屏。
>   这种情况点按钮会弹一行提示告诉你按 `F11` 或 `Esc`（**不会**误触发脚本自己的全屏）。
>   F11 的进入/退出只能由你自己按键（或用本脚本的 ⛶ 按钮进出，那个可以一键切换）。

### 面板说明

```
白天模式
┌────────────────────────────────────────┐
│  ☀ ──────●────────────────────    90%  │  ← 亮度：左右拖动整页明暗，右侧是百分比
│                                         │
│  颜色                                   │  ← 点色块立即生效
│   ◻白   ◻米黄   ◻青绿                  │
│                                         │
│  背景                                   │  ← 与颜色自由组合，两者都会即时预览
│   ◻纯色   ◻纸纹   ◻素纸   ◻云   ◻月   │
│                                         │
│                              [ 恢复默认 ]│
└────────────────────────────────────────┘

黑夜模式：没有面板 —— 整块交还官方原装外观
```

- **颜色**：**3 套底色** —— 白 `#f8f8fa` / 米黄 `#f5efd9` / 青绿 `#c0edc6`
- **背景**：官方手机版的 **5 种纹理** —— 纯色 / 纸纹 / 素纸 / 云 / 月，
  **每一种都单独适配了 3 套底色**（官方同一纹理在不同底色下色调不同），
  所以是 **3 × 5 = 15 种组合**，随便配（「月」就是官方那个带月亮的夜空插画）
- **亮度**：拖动时实时预览，松手后保存；右侧百分比便于把同一套设置复现到别的机器
- **选中状态**：当前生效的色块有金色描边
- **关闭面板**：点面板外的灰色遮罩、按 `Esc`、按 `b`、或再点一次 ☀
- **快捷开关**：任何时候按 **`b`** 开关面板（在输入框/写想法的地方不会误触发；
  黑夜下面板不存在，按 `b` 也不会有反应）

**所有设置自动保存**，下次打开自动恢复，不用重新设置。

> **关于配色来源（v1.6.0 → v3.0.0）**
>
> 手机版的背景是公认最舒服的，所以这套配色不是凭感觉调的，而是**从官方截图逐像素采样**得到
> （40 张 = 4 颜色 × 5 背景 × 2 轮）。
>
> 关键发现：官方背景**同一种在不同颜色下色调不同**（背景「云」在白色下是蓝天、米黄下是暖灰、
> 青绿下是淡绿），所以每个背景按颜色各存了一份实测值。
>
> | 底色（背景·纯色） | 白 | 米黄 | 青绿 |
> |---|---|---|---|
> | 官方实测 | `#f8f8fa` | `#f5efd9` | `#c0edc6` |
> | 正文字色 | `#0d141e` | `#3d3427` | `#2f3a2c` |
>
> 官方的第 4 组是黑色（`#000000`，正文 `#959595`），但**脚本没有采用**：
> 它和另外三色差别太小 —— 同一纹理在黑白下几乎一样 —— 放进来只会让人以为「点了没反应」。
> 需要夜间阅读请直接用黑夜模式（官方原装外观）。
>
> 桌面版网页**不带**这些素材（整个页面只有 1 个外链图片，还是书封），所以用 CSS 渐变 + 噪点重现。
> 半透明层的取值方法是**算出来的，不是调出来的**：底色记 `B`、官方实测记 `T`，
> 层用 `alpha = 0.5` 时合成结果是 `0.5×层 + 0.5×B`，所以 **层 = 2T − B**。
> 脚本里每个渐变的两个端点都是这么反推的。
>
> **v1.8.0 顺手把安卓安装包（130 MB）解开了，结论是「本来就没有图可拿」**：
> 40 个 Hermes 字节码 bundle 里没有任何阅读背景的资源地址，`resources.arsc` 里也没有
> `reader_bg*` 之类的资源名 —— 只有一大堆 `config_color_reader_{black,white,yellow,green}_{00..101}`
> 的**颜色配置**。也就是说官方的背景是**引擎按配置程序化画出来的，不是位图**（官方的纹理图是照片，
> 但那是渲染层的贴图，不在包里）。这反过来确认了用 CSS 渐变重现是对的路子。

> **白天 / 黑夜怎么分工（v3.1.0）**
>
> 一句话：**脚本只管白天；一进黑夜就整块让位，只多一个「全屏」按钮。**
> **昼夜切换用站点原生的「深色」按钮**，脚本不自己造。
>
> | | 底色 / 正文 / 位图 / 弹层 | 「背景」按钮 | 「全屏」按钮 |
> |---|---|---|---|
> | **白天** | 全部由脚本画（白 / 米黄 / 青绿 × 5 纹理） | 显示 | 显示 |
> | **黑夜** | **官方原装，脚本一点都不碰** | 隐藏 | 显示 |
>
> 实现上就一句：进黑夜时脚本把 `data-wrbg` 摘掉，所有覆盖规则随之失效 ——
> 阅读区、正文颜色、位图 filter、弹层配色全部回到站点自己的深色主题。
> 「背景」按钮也靠这个属性隐藏（`html:not([data-wrbg]) .wrbg-theme-host { display: none }`），
> 「全屏」不带这个类，所以两种模式下都在。
>
> > **⚠️ 为什么不自己加一个白天/黑夜切换按钮？**
> >
> > v3.0.0 加过（现在已撤掉）。它做的事是直接翻 `body` 上的 `wr_whiteTheme` 类，
> > 结果**从黑夜切回白天时正文会变成「浅色字压在浅色纸上」，几乎看不见**。
> >
> > 原因是阅读器的正文有一部分是引擎**光栅化进 `<canvas>` 的位图**，字色在绘制那一刻就烤进去了。
> > 只翻一个 CSS 类**不会通知引擎重绘** —— 它还以为在黑夜，继续按浅色画字；
> > 而我们的浅色纸底已经铺上去了，两者一叠就糊了。
> > 站点原生的「深色」按钮走的是引擎自己的状态，重绘时机对，所以没这个毛病。
> >
> > **结论：昼夜这个开关必须留给站点自己**，脚本只跟着它的类变化决定「接管」还是「让位」。
> > 顺带一提，脚本也不允许自己「点」那个原生按钮（那是模拟用户操作，会踩封号审查的红线）。
> >
> > **为什么不是「黑夜下脚本自己刷一层黑」？**
> >
> > v1.8.0 试过「黑夜下脚本只刷面」、v2.0.0 试过「把站点钉在浅色 + 藏掉深色按钮、黑色当第 4 色」，
> > 两条路都不行：
> > - 前者会和站点深色主题**抢同一片区域**，正文被两套规则来回改，字色就花了
> > - 后者等于把官方的黑夜模式整个废掉，而且黑那套和另外三色**差别太小**（同一纹理在黑白下几乎一样），
> >   放在白天模式里没有意义
> >
> > 所以现在回到最省事也最稳的分工：**黑夜完全交给官方。**

---

## 四、高级用法（可选）

在阅读页按 `F12` → Console，可以命令行调参：

```js
// 查看当前设置
wrbg.get()

// 精确设置（亮度 0~1）
wrbg.set({ colorId: 'sepia', bgId: 'paper', brightness: 0.9 })

// 可用的取值
wrbg.colors        // ['white','sepia','green']
wrbg.backgrounds   // ['solid','paper','plain','cloud','moon']
wrbg.state         // { colorId, bgId, brightness }
wrbg.dark          // 当前是不是黑夜（黑夜下脚本整块让位）

// 恢复默认
wrbg.reset()
```

---

## 五、常见问题

**Q：右侧工具条上没有 ☀ 按钮？**
- 先移动鼠标让工具条浮出来，按钮在「字号」下面
- 刷新页面（`F5`）
- 确认油猴里脚本是**已启用**状态，且地址是 `weread.qq.com/web/reader/...`
- 打开油猴面板看脚本有没有报错

**Q：双栏模式下、打开背景面板时，工具条背后有一条深色竖条？**
- **v1.1.3 已修复。** 原因是双栏模式下 `.readerControls` 带 `transform`，
  会让它成为遮罩（`position:fixed`）的包含块，遮罩被缩成了「工具条那一格」。
  现在改用负的 `100vw/100vh` 外扩，无论包含块是谁都盖满视口
- 若仍能看到，说明是别的元素，用 `工具/weread-probe2.js` 跑一份报告发我

**Q：改了颜色但正文没变？**
- 先确认脚本已更新到 **v3.0.0**，并刷新页面
- 双栏模式下背景不生效的问题已在 v1.1.1 修复
- 若仍不正常，微信读书可能改版调整了样式，用 `工具/weread-probe2.js` 跑一份报告发我

**Q：切到黑夜后正文颜色变了 / 半深半浅？**
- v3.0.0 起不该再出现：黑夜下脚本把 `data-wrbg` 摘掉，覆盖规则全失效，**一点都不碰站点**
- 如果还能看到，多半是脚本版本没更新（刷新页面，确认油猴里是 v3.0.0）
- 仍不行就用 `工具/weread-probe2.js` 跑一份报告发我

**Q：黑夜下按 `b` 没反应？**
- 正常。黑夜模式没有背景面板（背景功能只在白天），按钮也一并隐藏了

**Q：上下滚动时某一段文字颜色/亮度不一样？**
- 阅读器只把「当前视口那一屏」留成逐字 DOM，其余光栅化成 canvas 位图，所以滚动时是两种载体在交替
- 白天模式下脚本让两者用**同一个亮度系数**算字色，理论上逐位一致
- 若仍能看出接缝，说明站点改了渲染方式，反馈给我

**Q：会影响我的账号或阅读记录吗？**
- 不会。脚本只改本机浏览器的显示样式，不发任何额外请求，不碰书架、笔记和阅读进度

**Q：换电脑/换浏览器要重装吗？**
- 要。脚本装在浏览器里，但设置存在本机，两边互不影响

---

## 六、卸载

油猴面板 → 找到「微信读书网页版 · 背景颜色主题」→ 点删除。
阅读页会立刻恢复原样（设置记录可以用 `localStorage.removeItem('wrbg.settings.v1')` 清掉）。

---

## 七、关于排版功能（边距 / 行距 / 段间距 / 首行缩进）

**这部分已评估并明确不做。** 不是没实现，是在网页版上做不到：

1. **引擎里没有这些参数** —— `localStorage.wrLocalSetting` 只有 `fontFamily` / `fontSizeLevel`；
   原生面板只有「字号大小」和「字体」。
2. **CSS 变量改了没用** —— `--wr-reader-render-canvasXAround` 等改成 220px 后，
   矩形、滚动高度、canvas 像素指纹全部零变化，说明这些变量只写不读。
3. **排版结果被烘焙死** —— 每个字的位置写死在 `transform: translate(x,y)` 里，
   DOM 又只保留当前视口一屏，其余全是 canvas 位图。

唯一可行的办法是 hook 引擎的绘制调用或改它的压缩 JS，会破坏滚动高度与划线锚点定位、
且站点一更新就失效 —— **已决定不走这条路**。详见 `设计思路.md`。

**字号和字体请用站点自带的「字号」按钮**（A 图标），那里已经有 7 档字号和 29 款字体。
