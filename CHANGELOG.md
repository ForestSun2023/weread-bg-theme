# 更新日志

版本号跟脚本头部的 `@version` 一致。**每一次交付都必须先通过 `工具/交付检查.ps1` 的五道关卡**
（封号审查 / 功能回归 / 死代码 / 体积 / 像素验证），记录见 [`工具/审查记录.md`](工具/审查记录.md)。

完整的设计推导、实测数据与踩坑记录见 [`设计思路.md`](设计思路.md)。

---

## v3.2.0

- `@namespace` 改为作者自己的命名空间（`https://github.com/ForestSun2023`），`@author` 填上署名
- ⚠️ 升级提示：油猴用 `@namespace` + `@name` 一起识别脚本，**改了命名空间等于换了个新脚本**。
  请先在油猴面板里删掉旧条目再装新版，否则两条会并存、按钮变 4 个。
  设置不会丢（存在 `localStorage` 的 `wrbg.settings.v1`，键名未变）

## v3.1.1

- 黑夜下**额外清掉脚本写在 `<html>` 上的全部 `--wrbg-*` 内联变量**，做到零残留
- 从「承诺」变成「结构性事实」：以前靠「每条覆盖规则都记得写 `html[data-wrbg]` 作用域」，
  现在变量一清，就算以后有人新加规则忘了写作用域，黑夜下也不会串色
- 回归新增断言：黑夜下 `--wrbg-*` 内联变量必须为 0 条

## v3.1.0

- **撤掉脚本自造的「白天/黑夜切换」按钮**，昼夜只由站点原生的「深色」按钮控制
- 根因：阅读器正文有一部分是引擎**光栅化进 `<canvas>` 的位图**，字色在绘制时就烤进像素。
  只翻 CSS 类不会通知引擎重绘 → 从黑夜切回白天时正文仍是浅色字压在浅色纸上，几乎看不见。
  站点原生按钮走的是引擎自己的状态，重绘时机才对
- 修掉验证链路上 **4 个会让测试静默失效的 bug**（详见设计文档）：
  快照自带 CSP 拦掉 `fetch`、废弃 `$null` 变量在 `$ErrorActionPreference='Stop'` 下致命、
  断言看了被隐藏父容器的子元素、`-join` 绑定优先级把 82 条断言挤成一行
- harness 加保险：整段断言包进 `try/catch/finally`，抛异常也出报告并写栈

## v3.0.0

- **回到「脚本只管白天」**：一进黑夜就摘掉 `data-wrbg` 整块让位，只多一个「全屏」按钮
- 白天 3 色 × 5 纹理 = 15 种组合（黑色那套因「和另三色差别太小」而撤掉）

## v2.0.0

- 取消白天/黑夜双模式，黑色降为第 4 套底色，开放 4 × 5 = 20 种组合
- **新增第 5 道关卡「像素验证」**：把每种组合真渲染成 1240×2772 截图，
  按采样官方截图的同一套方法逐格比对 —— 起因是 v1.8.0「计算样式全绿、观感却很差」
- 用「层 = 2T − B」重算全部渐变端点；修掉「颗粒层浓度一刀切」（纯黑上噪点过强）

## v1.8.0

- 解包官方安卓安装包（130 MB，40 个 Hermes 字节码 + `resources.arsc`），
  **结论：官方背景不是位图**，是引擎按 `config_color_reader_*` 颜色配置程序化绘制 ——
  这反过来确认了用 CSS 渐变重现是对的路子
- 深色下把背景功能调回来（改为「只刷面、字交给站点」）

## v1.7.1

- 用第二批官方截图修正月亮几何：v1.7.0 把月亮放在右下角，实测官方在**右上角**且圆心压在顶边上方
- 「云」改为弥散的大椭圆亮区

## v1.7.0

- 用官方 20 张截图实现手机版的 **5 种背景**（纯色 / 纸纹 / 素纸 / 云 / 月）
- 关键发现：同一种背景**在不同底色下色调不同**（「云」在白下是蓝天、米黄下是暖灰、青绿下是淡绿），
  所以每个背景按底色各存一份实测值

## v1.6.0

- 按手机版官方色卡校准三套底色，纯色组合与官方**逐位一致**

## v1.5.1

- 按钮固定到所有原生按钮之后
- 深色下隐藏「背景」按钮（当时的设计是深色整块让位）

## v1.5.0

- **移除自家「夜间」配色**，深色交还站点。理由：和站点深色主题抢同一片区域，只会做出半深半浅

## v1.4.2

- 让站点原生「深色」按钮真正生效

## v1.4.1

- 按 ponytail（YAGNI）自我审查后瘦身

## v1.4.0

- 四项可用性改进：亮度百分比读数、快捷键 `b`、面板视口钳制、面板内事件不再冒泡

## v1.3.0

- 三项健壮性改进：首页 → 阅读页（前端路由）也能激活、主题失效自检告警、安装不再依赖阅读页

## v1.2.1

- F11 原生全屏的识别（无 API 可查、无事件可监听，只能推断）与「退不出」的明确提示

## v1.2.0

- 新增 ⛶ 全屏阅读按钮（全屏 `document.documentElement`，视觉上与 F11 等价）

## v1.1.3

- 修「双栏模式工具条背后一条深色竖条」：真因是**遮罩**被 `transform` 缩成了工具条那一格，
  改用负的 `100vw/100vh` 外扩

## v1.1.0 → v1.1.1

- 修双栏（横向）模式背景不生效：`.readerChapterContent` 有自己的白底且不在覆盖列表里

## v1.0.0 → v1.0.1

- 首版；修按钮注入抛 `NotFoundError`（站点原生的「深色」按钮嵌在 `.wr_tooltip_container` 里）
