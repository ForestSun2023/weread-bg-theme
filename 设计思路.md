# 微信读书网页版「阅读外观」油猴脚本 · 设计思路（已按实测修正）

> 本文的结论全部来自真机实测，不是推断。每次结论后面都附了证据。
> 交付物 = `weread-bg-theme.user.js`（亮度 / 颜色 / 背景）。
>
> **范围决策（已定）**：排版类功能（边距 / 行距 / 段间距 / 首行缩进）经评估在非侵入式前提下
> 无法实现，**明确不做**；不走 hook 引擎绘制调用那条险路。

---

## 一、结论先行

| 功能 | 可行性 | 依据 |
|---|---|---|
| 亮度 | ✅ 已实现 | 按系数压暗配色 RGB，不碰排版引擎 |
| 颜色（白/米黄/青绿/夜间） | ✅ 已实现 | `html[data-wrbg]` + CSS 变量 + `!important`，纵向/双栏均已验证 |
| 背景（纸张质感） | ✅ 已实现 | inline SVG 噪点 + CSS 渐变，无外部依赖 |
| 字号 / 字体 | ✅ 站点自带 | 原生面板已有：字号滑杆（7 档）+ 字体（29 款） |
| 边距 | ❌ **明确不做** | 引擎无此参数；CSS 变量只写不读；排版已烘焙进 transform |
| 行距 | ❌ **明确不做** | 同上，且可控的 DOM 只有一屏，其余是 canvas 位图 |
| 段间距 | ❌ **明确不做** | 同上 |
| 首行缩进 / 顶格 | ❌ **明确不做** | 需要重新断行，而排版结果已被光栅化 |

---

## 二、引擎的真实架构（四个实测事实）

### 事实 1 · 正文是「按字绝对定位」

```html
<span data-wr-id=wrk8nd243kljf data-wr-role=text
      class="wr_absolute ccn-s97uo5obrfn"
      style="transform:translate(441px,6457px);width:21px">沉</span>
```

每个字一个元素，坐标由引擎算好后**烘焙进 `transform`**，还会生成一条带随机 hash 的类：

```css
.readerChapterContent .ccn-s97uo5obrfn { font:21px/40px syht; height:30.666667938232422px; line-height:31px }
```

→ **改 CSS 的 `line-height` / `text-indent` 不会重排文字**，字还是待在原地。

### 事实 2 · DOM 只保留当前视口一屏，其余光栅化成 canvas

- 真机 `[data-wr-role="text"]` 数量 = **477**（一章约 3000 字，477 ≈ 一屏）
- `<canvas>` 2 个，挂在 `.wr_canvasContainer`（`position:absolute; z-index:2`，高度 = 整章 8643px）
- 每个 canvas CSS 高约 2630px，内部缓冲区 3 倍分辨率（`width=1563 / height=7859`），带 `data-random` 属性
- **本地验证（决定性）**：把 canvas 设为 `display:none` 后，章节标题仍在（变成我设的浅灰色），**正文段落全部消失**
  → 正文确实是 canvas 画的，滚过的区域也是

### 事实 3 · `--wr-reader-render-*` 是引擎的**输出**，不是输入

引擎把排版参数写在 `<html style>` 上：

```
--wr-reader-render-canvasTop:84px        --wr-reader-render-canvasXAround:70px
--wr-reader-render-canvasBottom:70px     --wr-reader-render-canvasXBetween:50px
--wr-reader-render-fontSizeLevel:2px     --wr-reader-render-fontFamily:syhtpx
```

把它改成 `220px` 后，逐项测量：

| 测量项 | 改前 | 改后 | resize 后 | 双栏切换后 |
|---|---|---|---|---|
| canvas 矩形 | — | 无变化 | 无变化 | 无变化 |
| renderTarget 矩形 | — | 无变化 | 无变化 | 无变化 |
| 滚动高度 | 9071 | 9071 | 9071 | 9071 |
| canvas 像素指纹 | — | 未变 | 未变 | 未变 |

→ **改 CSS 变量对排版零影响**，「写变量 + 触发重排」这条路彻底不成立。

### 事实 4 · 引擎根本没有行距/缩进/边距这些概念

- `localStorage.wrLocalSetting = {"fontFamily":"syht","fontSizeLevel":2}` —— **只有这两项**
- 原生「字号」面板的全部内容：`字号大小`（7 档滑杆）+ `字体`（全部 29 / 仓耳 12 / 方正 7 / 其他 9）
- 全页检索 `边距` `行距` `缩进` `首行` `段间距` —— **命中 0 次**

→ 不是网页版没做 UI，是**引擎内部压根没有这些参数**。

---

## 三、为什么「自己重排」也不行

原本的兜底方案（改写 transform 实现行距/缩进）在事实 2 面前失效：

| 想做的 | 为什么会失败 |
|---|---|
| 改行距 | 只能改到当前一屏的 transform；滚过去的部分是 canvas 位图，会和新行距错位对不上 |
| 改首行缩进 | 需要重新断行（首行少 2 格就要把 2 个字挤到下一行并级联），而断行结果已被光栅化 |
| 改边距 | 同上，且引擎下次重排会覆盖 |
| 撑高容器 | canvas 分块高度是引擎写死的，改了自己也画不出对应内容 |

**唯一剩下的路是改引擎本身**（hook `CanvasRenderingContext2D` 的绘制调用、或改它的压缩 JS）。这条路：
- 站点一更新就可能失效
- 会破坏滚动高度、划线锚点、笔记定位
- 属于对第三方站点的侵入式修改

**已决策：不走这条路。** 排版类功能就此放弃，脚本范围锁定在「亮度 / 颜色 / 背景」。

---

## 四、v1 已修正的 bug（v1.0.0 → v1.0.1）

**症状**：按钮不出现，但原生的「深色」按钮消失了（说明脚本已启动、CSS 已生效）。

**根因**：站点的「深色」按钮不是 `.readerControls` 的直接子节点，被包在 `.wr_tooltip_container` 里。
`controls.querySelector('.readerControls_item.dark')` 拿到的是**后代**按钮，拿它当 `insertBefore` 的参照节点会抛异常：

```
Uncaught NotFoundError: Failed to execute 'insertBefore' on 'Node':
The node before which the new node is to be inserted is not a child of this node.
```

**修复**：定位到深色按钮所在的**直接子节点**再插入，并加 `try/catch` 兜底，任何插入异常都退回 `appendChild`。

**验证**（本地单文件预览 + headless 渲染，三套配色全部通过）：

```
data-wrbg = sepia / bg=paper     .wrbg-btn = 1   .wrbg-panel.open = 1
controls children = … | reader-font-control-panel-wrapper | wrbg-wrap | wr_tooltip_container
body bg = rgb(239,232,214)    text color = rgb(60, 51, 39)    errors: (none)

data-wrbg = night / bg=solid
body bg = rgb(27, 27, 29)     text color = rgb(169, 169, 174)  errors: (none)
```

配色数值与预设完全吻合（米黄 `#f6efdd` × 0.97 = `rgb(239,232,214)`）。

### v1.1.0 → v1.1.1 · 双栏（横向）模式背景不生效

双栏模式用的是**另一套 DOM**，和纵向模式几乎没有交集：

```
class=app_content   = 0     ← .app_content 不存在
class=readerContent = 0     ← .readerContent 不存在
data-wr-role=text   = 0     ← 没有逐字 span
主体 = .wr_horizontalReader > .wr_horizontalReader_app_content
```

排查过程中踩了两个坑，记下来避免重犯：

1. **误判 canvas**：一度以为横向模式的 canvas 是「自带不透明白底的整页位图」，还给它加了染色滤镜。
   后来**解码存档里的 canvas 位图**才确认：位图 `1683×1992`，采样 650 点 ——
   全透明 610、**不透明白 0**，只有字形。两个 canvas 都是如此。
   → canvas 是**透明字形层**，绝不能给浅色主题加染色滤镜（会把正文字染变色）；
   夜间保留 `invert(1) hue-rotate(180deg)` 是必要的。

2. **误判 transition**：headless 的虚拟时间会冻结动画时钟，所有带
   `transition: background-color .2s` 的元素 computed 值卡在白色，看起来像规则失效。
   关掉 transition 后全部正常。**这是测试环境假象，不是真 bug。**

**真正的根因**：`.readerChapterContent` 自己有一层白底（双栏下是 `1178×678` 一大块），
而它之前只被我当作**文字颜色**的目标，没进背景容器列表，于是它盖住了下面已经变色的容器。

**修复**：把 `.readerChapterContent` 加进背景容器列表。像素级验证：

| 采样点 | 修复前 | 修复后 |
|---|---|---|
| 卡片内-中 | `255,255,255` | `246,239,221` ✅ |
| 卡片内-左下 | `255,255,255` | `246,239,221` ✅ |
| 卡片外 | `246,239,221` | `246,239,221` ✅ |

双栏 + 夜间同样验证通过（全部 `27,27,29`，canvas 反相后深底浅字）。

---

## 五、当前状态

**已完成并验证（v3.1.0）**

- ✅ 工具条 ☀ 按钮 + 面板（亮度滑杆 / 颜色 3 套 / 背景 5 种 / 恢复默认）
- ✅ **白天 3 色 × 5 纹理 = 15 种组合全部可选**
- ✅ **黑夜模式整块交还官方原装**：摘掉 `data-wrbg`，脚本一点不碰
- ✅ **昼夜切换只用站点原生的「深色」按钮**，脚本不自己造（v3.1.0，见下）
- ✅ **5 种背景 = 官方手机版的「纯色 / 纸纹 / 素纸 / 云 / 月」**（v1.7.0）
- ✅ 工具条 ⛶ 全屏阅读按钮（全屏 `document.documentElement`，见第八节）
- ✅ 两个按钮固定在所有原生按钮之后（v1.5.1）
- ✅ 设置持久化，`Esc` 或 `b` 或点遮罩关闭，tooltip 仅悬停显示
- ✅ 纵向阅读：底色、正文色、顶栏、悬浮按钮、目录/笔记/字号弹层
- ✅ 双栏（横向）阅读：容器 + `.readerChapterContent` 全部覆盖
- ✅ 双栏下工具条背后不再有深色竖条
- ✅ 首页 → 阅读页（前端路由切换）也能激活（v1.3.0）
- ✅ 主题失效自检告警（v1.3.0）
- ✅ 亮度百分比读数 / 快捷键 `b` / 面板视口钳制（v1.4.0）
- ✅ 无控制台报错
- ✅ **交付检查 5 关全过**：封号审查 5 类全 0、回归 82 项全绿（41 × 2 快照）、
      死代码 32 函数全有调用、像素验证 15 种组合全达官方实测（v3.1.0）

### v3.1.0 · 撤掉自家切换按钮（它会把正文写坏），并修掉测试链路上的三个哑巴 bug

**起因（用户反馈 + 截图）**：从黑夜切回白天后正文变成「浅色字压在浅色纸上」几乎看不见；
而且站点的原生「深色」按钮没被隐藏，工具条上出现了**两个**切换按钮。
用户判断：**保留原装按钮，脚本不要自己造**。判断是对的。

#### 一、产品侧的根因：只翻 CSS 类，不会通知阅读引擎重绘

v3.0.0 那个按钮做的事就是 `document.body.classList.toggle('wr_whiteTheme')`。
但阅读器的正文有一部分是引擎**光栅化进 `<canvas>` 的位图** —— 字色在绘制那一刻就烤进像素里了。
只翻一个 CSS 类，引擎不知道要重绘，于是它还以为在黑夜、继续按浅色画字；
而我们的浅色纸底已经铺上去，两者一叠就成了截图里那种「糊」。

站点原生的「深色」按钮走的是引擎自己的状态，重绘时机是对的，所以没这毛病。

**结论：昼夜这个开关必须留给站点自己。** 脚本只跟着它的类变化决定「接管」还是「让位」。
`apply()` 上方现在留了一段 ⚠️ 注释把这件事写死，防止以后再有人「顺手加个切换按钮」。

> 顺带一提：也不能靠「脚本去点那个原生按钮」绕过 —— 那是模拟用户操作，会踩封号审查的红线。

#### 二、测试侧：交付检查连续 4 次全绿，其实是**一条断言都没跑**

v3.1.0 的代码改对了，但 `工具/回归验证.ps1` 从 23:49 起就一直输出「0 项通过 / 0 项失败」。
排查出**四个叠在一起的哑巴 bug**，每个都会让测试**静默失效**（这比报错危险得多）：

| # | bug | 症状 | 修法 |
|---|---|---|---|
| 1 | 快照自带的 **CSP** | SingleFile 把站点的 `<meta http-equiv=content-security-policy>` 原样存了下来，内容是 `default-src 'none'` 且**没有 `connect-src`** → `connect-src` 回退到 `'none'`，页面里**任何 `fetch` 都被浏览器掐掉**。上一个会话刚把结果回传机制从「读 stdout」改成「页面 POST `/report`」，正好撞上 | 构建测试页时摘掉 CSP meta（本地夹具，与要验证的东西无关） |
| 2 | 废弃变量 `$realDump` | 上一版机制的残留，恒为 `$null`；`Remove-Item $null` 在 `$ErrorActionPreference='Stop'` 下是**致命错误** → 脚本在第一个快照后整个中止，而所有输出都在结尾才打印，外面一片空白 | 删掉那一行 |
| 3 | 断言看错了对象 | 「黑夜只多一个可见按钮」数的是按钮自己的 `computed display` —— 被隐藏的是**父容器**，子按钮照样是 `flex`，于是误报成 2 个 | 改用 `getClientRects().length`（真·渲染出来没有） |
| 4 | `-join` 绑定优先级 | `$report += "头" + (数组) -join "`n"` 把整个拼接结果当数组 join，`$report` 里塞进的是**字符串数组**，`Write-Host` 用空格分隔打印 → 82 条断言挤成一行，交付检查的 `^\s+PASS` 一条都数不到 | 先算出 `$detail` 字符串再 `+=` |

另外给 harness 加了两道保险，避免下次再「静默失效」：
- **整个断言过程包进 `try/catch/finally`**，抛异常也要出一份报告并把栈写进去
  （之前某条断言对 `null` 调方法 → 直接跳出 async IIFE → `finish()` 永不执行 → 外面只看到「拿不到诊断结果」）
- **诊断框找不到就自己造一个**，不让 `finish()` 自己抛异常
- 「拿不到诊断结果」的提示里直接列出排查顺序（先查 CSP，再用 `-Keep` 截图看黄色诊断框）

**教训：测试链路的失败模式和产品代码不同 —— 它经常是「静默通过」而不是「报错」。
所以测试脚本本身也要有「跑没跑起来」的证据，并且要把这个证据当成一等公民。**

### v3.0.0 · 回到「脚本只管白天」，并补一个自己的切换按钮

**起因（用户原话）**：黑色背景区别太不明显 → 白天模式去掉黑色；
把白天/黑夜模式加回来，要求跟之前一样；黑夜模式**纯用官方原装**，
脚本只多一个「切换模式」按钮和全屏按钮，**不要背景功能**；
之前切到黑夜会受到白天模式的干扰导致文字变成其他颜色。

**三次试错的完整轨迹**（值得记下来，因为每次都是"看起来更聪明"的方案反而更差）：

| 版本 | 黑夜怎么处理 | 结果 |
|---|---|---|
| v1.5.0 – v1.7.1 | 完全让位（摘 `data-wrbg`），并隐藏「背景」按钮 | ✅ 稳，但黑夜下用不了背景功能 |
| v1.8.0 | 只刷「面」刷成纯黑，字色交给站点 | ❌ 和站点深色主题抢区域，正文被两套规则来回改，字色花掉 |
| v2.0.0 | 把站点钉在浅色 + 藏掉深色按钮，黑色当第 4 色 | ❌ 等于废掉官方黑夜；且黑色与另三色差别太小，像"点了没反应" |
| **v3.0.0** | **完全让位 + 自己提供一个切换按钮** | ✅ 回到稳的方案，同时补上"想切黑夜"的入口 |

**结论：两条路都不如「一条路 + 一个开关」。**
站点自己的深色模式是完整的（面板、插图、正文位图逐帧重绘全都有），
脚本既不该跟它抢区域，也不该把它藏掉 —— 该做的是**给用户一个方便的开关**，
然后把那片区域整个让出去。

**实现要点**

1. `apply()` 第一句就是黑夜判断，命中就摘掉 `data-wrbg` 直接 return：
   ```js
   if (siteIsDark()) { root.removeAttribute('data-wrbg'); syncModeButton(); return; }
   ```
   所有覆盖规则都挂在 `html[data-wrbg]` 上，属性一摘全部失效 ——
   阅读区、正文颜色、位图 filter、弹层配色一次性全部回到站点原装，不需要逐条撤销。
2. **「背景」按钮的隐藏不需要额外状态**：CSS 里那条
   `html:not([data-wrbg]) .wrbg-theme-host { display: none !important; }` 就够了。
   「切换模式」和「全屏」不带 `wrbg-theme-host` 类，所以在两种模式下都留着。
3. **「切换模式」按钮不缓存状态**：它只翻 `body` 上的 `wr_whiteTheme`，
   而站点原生的「深色」按钮改的是同一个类 —— 所以两者永远同步，谁都不会覆盖谁。
   图标/提示由 `syncModeButton()` 按当前状态反向显示（白天显示月亮 = 点了去黑夜，跟全屏按钮同一套约定）。
   按钮本身也**不隐藏站点那个原生深色按钮**（v2.0.0 藏过，现在恢复）。
4. 删掉的东西：`ensureLightMode()`、`mutatingBodyClass` 自激保护（脚本不再写 body 的类了）、
   `black` 配色与它的 `invert` / `noise` 字段、`noiseLayer()` 参数化、4 种纹理里的 `black` 行。
   → 体积从 45,907 回到 **44,806 字节**。
5. canvas 亮度 filter 现在统一用 `brightness(fgK)`（和 DOM 文字同一个系数），
   不再有黑色专用的 `invert(1) brightness(.584)` 分支。

**「切换模式」按钮的图标不会和「背景」按钮撞车**：太阳图标是共用的 `THEME_ICON`，
但「背景」按钮在黑夜是隐藏的（那时才显示太阳），白天显示的是月亮 —— 两者永不同框。

### v2.0.0 · 取消双模式，黑色降为第 4 色，并补上「像素验证」

**起因**：v1.8.0 的深色模式「效果不好」。复盘后确认问题不在实现细节，而在**方案本身**：
「站点的深色主题」和「脚本自己的底色」是两条互相打架的路 ——
站点一切深色就按它自己的主题重绘正文位图，脚本的反相假设随之全错位。

**新方案：只留一条路，四套底色平级。**
- 配色表去掉 `mode` 字段，黑色变成普通第 4 套，和另外三色一样能配全部 5 种纹理
- 站点那个「深色」按钮直接 `display:none`，并且每版 `apply()` 都把站点钉回浅色
- 于是「4 色 × 5 纹理 = 20 种组合」成为唯一的状态空间，没有模式分支

**关键技术点：位图反相的字色要和 DOM 逐位对齐。**
引擎只会画深色字（它以为自己在浅色纸上）。压到纯黑上等于看不见，所以要把 canvas 整层反相。
但反相出来的灰必须和 DOM 那一屏的字色**完全一样**，否则滚动时会出现「两种灰」的接缝 ——
这大概正是 v1.8.0「效果不好」的观感来源之一。所以：

```
官方暗色截图正文区实测最亮 400 点均值 = 149（峰值也只有 150）→ 官方夜间字色是 #959595
站点浅色主题正文字实测 = 纯黑 #000000
⇒ invert(1) 把纯黑映射成纯白，再乘 149/255 = 0.584，正好落回 149
⇒ --wrbg-text = #959595（DOM），--wrbg-canvas-filter = invert(1) brightness(.584)（位图）
```

亮度滑杆复用同一个 `fgK` 系数，两边永远同步。
顺带把浅色三套的 canvas 亮度也从原来那个独立公式（`0.55+0.45b`）改成同一个 `fgK` ——
原先它和 DOM 用的是两个公式，拖动亮度时同样会有轻微接缝。

**黑色那 5 种纹理的实测值**（官方暗色截图批 2，左侧页边避开正文；批 1 的 70–95% 段被 UI 污染，弃用）：

| 纹理 | 官方实测（顶 → 底） | 做法 |
|---|---|---|
| 纯色 | `0,0,0` 平坦 | 无图层 |
| 纸纹 | `(4,4,4)` → `(5,6,6)` | 极淡颗粒 + 微亮渐变 |
| 素纸 | `(3,3,3)` 平坦 | 只有极淡颗粒 |
| 云 | `(12,13,15)` → `(20,33,41)` | 底部偏蓝的弥散亮区（官方底部其实是一张雪山照片） |
| 月 | 靛蓝 `(7,11,20)` → `(7,9,16)`，右上峰值 250 | 深靛蓝 + 右上柔边椭圆月亮 |

**两个真实缺陷（都是像素验证抓出来的，计算样式全绿看不出来）**：
1. **颗粒层浓度不能一刀切**：纸纹/素纸的噪点原本固定 `opacity 0.10`，
   在浅色纸面上刚好，压到纯黑上就把官方的 `(5,6,6)` 抬到了 `16`。
   于是给配色表加 `noise` 字段，黑色单独用 `0.03` 那一档。
2. **白·云的纵向渐变几乎是平的**：原实现的线性渐变两端只差 8，渲染出来顶→底只亮 3 个色阶，
   而官方是 `194,214,232 → 228,235,242`（亮 22 个色阶）。
   重算了全部渐变为 `层 = 2T − B`（见下），白·云现在实测 `215,224,233` vs 官方 `213,224,235`。

**半透明层的取值方法（统一成一条公式）**：底色记 `B`、官方实测记 `T`，
层用 `alpha = 0.5` 时合成结果是 `0.5×层 + 0.5×B`，所以 **层 = 2T − B**。
脚本里每个渐变的端点都是这么反推的（少数非线性剖面再加一个实测折点，如青绿·月）。

**新增第 5 道关卡：像素验证（`工具/像素验证.ps1`）**
回归验证只断言计算样式 —— 而 v1.8.0 恰恰是「计算样式全绿、观感很差」。
所以补一关：把 20 种组合真渲染成 1240×2772 截图，用**和当初测官方截图完全一样**的采样法
（左侧页边 3 段亮度）逐格对官方实测值，另外再验三条结构：
- 云的走向：底段 − 顶段 ≥ 门槛
- 月亮位置：月亮盒（x 84–92%，y 5–9%）相对**同一张图的顶部页边**的抬升 ≥ 官方实测的 60%
- 色调方向：米黄偏暖（R>B）、青绿偏绿（G>R）

踩到的两个测试坑（都已写进脚本注释）：
- `$js` 注入页面时**必须包 `<script>`**，否则整段被当文本，20 格全返回同一个值
- `@(Lum $a, Lum $b, Lum $c)` 会被 PowerShell 当成**一条命令传三个参数** → 空数组 → 伪造一堆假 FAIL。
  必须逐条调用。
- 月亮盒不能从 y 0 起算：快照顶部是站点外层导航栏，搜索图标正好压在月亮位置

### v1.8.0 · 解开安卓包 + 深色下把背景功能调回来

**需求**：① 把官方黑色背景做进暗色模式；② 把深色下的背景功能调回来；
③ 白天只有现有的三种配色；④ 黑夜只有黑色。

**第一步：解包找官方背景素材（结论：没有）**

用户给了 `base.apk.1.apk.1_tmp.apk.1_tmp`（130 MB，合法 ZIP，7152 项）。逐层排查：

| 查了什么 | 结果 |
|---|---|
| `assets/assets/drawable-*/src_*.png`（RN 图集，3713 项） | 全是 UI 图标；按 `bg/read/paper/theme/night/dark/moon/cloud/skin/texture` 过滤后无阅读背景 |
| 40 个 `.hbc`（Hermes 字节码，29 MB，含整个 reader 逻辑） | 字符串表里**没有任何背景图的资源地址** |
| `resources.arsc`（14783 个标识符） | 没有 `reader_bg*` 之类的资源名；只有 `config_color_reader_{black,white,yellow,green}_{00..101}` 一族**颜色配置** |
| `res/` 里最大的 25 个文件 | 全是 UI 图 / GIF，无纹理 |

**结论：官方的阅读背景是引擎按颜色配置程序化绘制的，不是位图。**
这反过来确认了脚本用 CSS 渐变重现是对的路子 —— 贴图方案在官方那里根本不存在，
所以「要不要内嵌像素级真实贴图」这个悬而未决的问题**自动作废**了。

**第二步：用官截拿深色底色的真值**

40 张截图（两批，各 20 张 = 4 颜色 × 5 背景）按主体色聚类，黑组 5 张采样全是 `0,0,0`。
于是「官方黑色背景」= 纯黑 `#000000`。

**第三步：关键设计 —— 深色下不许抢字色**

v1.5.0 砍掉自家夜间配色的理由仍然成立：站点深色模式本来就完整，脚本再压一层就是半深半浅。
但用户要的是「深色下也能调背景」，于是让步让不成、抢又抢不得，**只能把「面」和「字」拆开**：

```
                 底色          顶栏/底栏/悬浮按钮   正文颜色        canvas 位图
白天           脚本给浅色      脚本                脚本            脚本跟随亮度
深色           脚本刷纯黑      脚本                站点深色主题    站点重绘（不叠 filter）
```

实现上只用 `data-wrbg-mode="dark"` 这一个根属性把两套 CSS 规则分开：
`html[data-wrbg]:not([data-wrbg-mode="dark"])` 只管白天的字色 / 弹层字色，
面那一批 `html[data-wrbg]` 两种模式共用。canvas 的亮度 filter 在深色下必须为 `none` ——
位图是站点按深色主题重绘的**浅色字**，再叠 brightness 只会把字压灰。

**第四步：可选项按模式收窄**

配色表加 `mode` 字段（3 套 `light` + 1 套 `dark`），颜色行只列当前模式的那几套；
深色下 `bgId` 强制走「纯色」、「亮度」固定按 1 算。
所以深色面板只剩「颜色 · 黑」+「恢复默认」—— 亮度行和背景纹理行由 CSS 整块收起
（纯黑再压还是黑；浅色纹理压在黑上只会发灰）。
模式切换靠 `apply()` 里的 `renderedMode` 比对触发重画，站点「深色」按钮的
body-class 观察器已经天然把它带上了。

**顺手砍掉的死代码**：`MOON_SVG` 和 `.wrbg-swatch .wrbg-moon` ——
它们唯一的用处是 `if (c.dark) …` 那个分支，而自从 v1.5.0 移除夜间配色后
就没有任何配色带 `dark` 字段了。

**踩到的坑（值得记下来）**：交付检查脚本本身突然报语法错、中文全变乱码。
根因不在脚本，是**宿主换成了 Windows PowerShell 5.1 + ANSI 代码页 936**，
而三个 `.ps1` 是**无 BOM 的 UTF-8** —— 5.1 用 ANSI 代码页解码，中文串直接崩坏。
修法是给 `.ps1` 加 UTF-8 BOM（PS 5.1 认 BOM）。
**教训：本机 PowerShell 脚本一律带 BOM，否则「中文注释的测试脚本」会无预警失效。**

### v1.7.1 · 用第二批官截修正月亮几何

**用户又提供了一批更完整的官方截图**（20 张，仍是 4 色 × 5 背景，但裁切更完整、能把背景看全）。

**先做结构还原**：新批次的图形位置在三种浅色之间**完全一致**（只是换色），说明图形是固定的 ——
所以只需测一次几何。

**程序化测光团**（按亮度取峰值区域求质心与包围盒）：

```
背景⑤(月)  光团中心 x≈88~92% / y≈2.4~3.4%   可见宽约 260px（≈阅读区宽的 21%）
背景④(云)  没有明确小光团，是「下半部一大片柔光」（质心随颜色漂移，说明是弥散亮区）
```

**发现并修正一处几何错误**：v1.7.0 我把月亮放在**右下角**（`circle at 80% 84%`），
而官方在**右上角**、且圆心基本压在顶边上方（只露出下半轮）。
改为 `radial-gradient(ellipse 10.5% 9% at 89% 1%, …)`；「云」也改成弥散的大椭圆亮区。

**验证**：三色渲染后裁出右上角，月亮都准确落在右上、被顶边切掉下半 —— 与官截一致。

### v1.7.0 · 用官方 20 张截图实现手机版的 5 种背景

**用户提供了官方截图**（`官方背景截图/`，20 张 1240×2772）。

**先做结构还原**：把 20 张的色卡行拼成一张对照图，读出每张的选中项 ——
**20 = 4 颜色 × 5 背景的完整组合**（文件顺序即组合顺序，边框颜色还跟着主题走）。

**关键发现**：官方背景**不是纯色/纸纹，而是插画**（蓝天、云、月亮），
而且**同一种背景在不同颜色下色调不同**：

| 背景 | 白色 | 米黄 | 青绿 |
|---|---|---|---|
| ④ 云 | `#d1e1f1`（蓝天）| `#e6ddd8`（暖灰）| `#d4f9da`（淡绿）|
| ⑤ 月 | `#b6cfe5` | `#e5dbd2` | `#b2d9bc` |

所以不能只存一套值 —— **每个背景按颜色各存一份实测值**（`per: { white, sepia, green }`）。
`BACKGROUNDS` 增加 `per` 字段，配套 `bgImage/bgSize/bgRepeat` 三个取值函数。

**半透明反推**：叠加值仍按 `tint = 2×target − base`（a = 0.5）求解，
个别需要更强覆盖的（青绿+素纸）用 a = 0.75。用半透明而非不透明贴图的理由：
**亮度滑杆必须仍然有效**（不透明贴图会把压暗的底色完全盖住）——这是 v1.6.0 就定下的红线。

**验证**（逐组合渲染 + 采样，与官方值对比，`Δ` = 三通道绝对差之和）

```
white,solid  #f8f8fa  #f8f8fa  Δ0 ✓     sepia,solid  #f5efd9  #f5efd9  Δ0 ✓
white,cloud  #d1e1f1  #d1e1f1  Δ0 ✓     green,solid  #c0edc6  #c0edc6  Δ0 ✓
white,moon   #b9d1e5  #b6cfe5  Δ5       sepia,cloud  #e8dfd9  #e6ddd8  Δ5
white,paper  #f8f8f8  #fafafa  Δ6       green,cloud  #d5f6da  #d4f9da  Δ4
sepia,paper  #f4e6cb  #f9eacd  Δ11      sepia,plain  #f5f1ea  #f9f4ee  Δ11
white,plain  #f4f5f7  #f7f8fa  Δ9       green,moon   #bedec6  #b2d9bc  Δ27
```

**三个纯色逐位一致**。带背景的组合残留几个色阶的偏差，来自两个已知因素：
① 噪点层把浅色压暗约 4/255；② 官截 2772px 高、渲染窗口 900px 高，
同一个 y 落在渐变的不同位置（所以对比本身有系统偏移）。

**没做到的部分（如实说明）**：云与月是**用 CSS 径向渐变近似**的，不是官方原图。
要拿到原图必须解包 App —— 桌面版网页确实不带（全页仅 1 个外链图片，还是书封）。

### v1.6.0 · 按手机版官方色卡校准配色

**需求**：手机版的背景最好最舒服，能否找到官方背景资源并实现。

**先查「官方资源在哪」**：把两份页面存档全文检索 —— `cdn.weread` / `/bg/` / 纹理类字段全部为 0，
**整个页面只有 1 个外链图片**（`wfqqreader-1252317822.image.myqcloud.com` 上的书封）。
结论：**桌面版网页确实不带这些素材**，它们只打包在手机 App 里。

**改用「从官方截图采样」**：用户提供的手机版截图（1240×2772）里就有官方色卡。
用 System.Drawing 程序化定位色卡行与色块边界（扫行/扫列找非面板底色区域），逐块采样：

| 项 | 官方采样值 |
|---|---|
| 颜色·白 / 米黄 / 青绿 | `#ffffff` / `#f7f2df` / `#ccefcf` |
| 背景·牛皮 | `#fce5c3` |
| 背景·淡粉 | `#faf3eb` |
| 背景·暖橙 | `#ffdeb5 → #ffc48a`（竖直）|
| 背景·灰纹 | `#fef4e8 → #e6dace`（斜向）|
| **实际阅读页（米黄+羊皮纸）** | **`#f8e9cc`** |

最后一行信息量最大：它就是用户每天看的那个纸面色，比色卡本身更值得对齐。

**实现**：保持**半透明**图层，把 alpha 按「叠在米黄 `#f7f2df` 上 ≈ 官方采样值」反推（取 a = 0.5）。
例如暖橙：`target = base×0.5 + tint×0.5` → `tint = 2×target − base`，得 `rgba(255,202,139,.5)`。
这么做的两个理由：

1. **亮度滑杆仍然有效** —— 若把背景做成不透明，底色一压暗就被完全盖住（这是本轮特意避开的坑）
2. 配色与背景仍可自由组合

**验证**（渲染后逐点采样，与官方值对比）

```
sepia,solid → #f7f2df  = 官方米黄 ✓ 逐位一致
green,solid → #ccefcf  = 官方青绿 ✓ 逐位一致
white,solid → #ffffff  = 官方白   ✓ 逐位一致
sepia,paper → #f4e6cc  vs 官方 #f8e9cc  Δ(4,3,0)   ← 噪点层略压暗
sepia,pink  → #f9f2ea  vs 官方 #faf3eb  Δ(1,1,1)   ✓
```

纯色三项**逐位一致**；渐变项因噪点层差异在 4/255 以内，肉眼不可辨。
（顺带把噪点 opacity 从 .16 调到 .10，贴近官方那种很细的纸纹。）

### v1.5.1 · 按钮固定到末尾 + 深色下隐藏背景按钮

**反馈**：截图显示深色模式下脚本加的两个按钮图标几乎看不见，且它们夹在原生按钮中间。
要求：① 两个按钮放到所有原生按钮之后（固定）；② 深色下背景功能不启用、**按钮也不显示**；
③ 原装深色最好用，不要额外改变。

| 项 | 改前 | 改后 |
|---|---|---|
| 按钮位置 | 插在「深色」按钮**之前** | **`appendChild` 到工具条末尾** |
| 深色下「背景」按钮 | 照常显示，但点了没用 | **整块隐藏**（`html:not([data-wrbg]) .wrbg-theme-host`）|
| 深色下「全屏」按钮 | `currentColor` 继承成暗色 → 隐形 | 显式 `#858c96`（站点自己的图标灰）|
| 深色下面板 | 用 `DARK_PANEL_VARS` 把面板调深 | **删掉整组变量** —— 按钮已隐藏，面板不可达，快捷键一并禁用 |
| 深色下阅读区 | 摘掉 `data-wrbg` 让位 | 不变（原装深色不做任何额外改变）|

**又一次 ponytail 式收获**：把按钮移到最后，`findDarkSlot()` 立刻变成死代码删掉；
把「背景」按钮在深色下隐藏，`DARK_PANEL_VARS` 也变成死代码删掉。
**两个「砍掉一个入口」的动作各消灭了一段绕行逻辑** —— 净减 98 字节，函数 30 → 29 个。

### v1.5.0 · 移除自家夜间配色，深色交还站点

**决定**：用户反馈自家夜间配色「使用体验不好」，移除 **夜间** 预设。

**根因**：自家夜间配色是在跟站点自带的深色模式**抢同一个状态**，只能靠
`!important` 硬压颜色 + 给 canvas 位图做 `invert(1)` 反相，做出来是半深半浅的效果。
而站点的深色模式本来就完整得多（面板、插图、canvas 逐帧重绘都有）。

**新分工**：脚本只负责**浅色纸面**；深色由站点负责，脚本自动配合。

| 事件 | 脚本行为 |
|---|---|
| 你点站点「深色」 | 摘掉 `data-wrbg` → **所有覆盖规则自动失效**，阅读区交还站点；只把自己的面板变量换成深色（免得刺眼） |
| 你点回浅色 | 恢复 `data-wrbg` → 重新接管，恢复上次那套纸面 |
| 深色下从面板选配色 | 视为「我要浅色阅读」→ `ensureLightMode()` 切回浅色再应用 |

**实现要点**

- 用「摘掉 `data-wrbg`」来让位，而不是再写一套 `:not()` 选择器 —— 一条现有规则体系
  自动整体失效，改动量最小
- `ensureLightMode()` 是全脚本**唯一**写 `body` 类的地方，且只在用户从面板主动操作时调用，
  所以站点「深色」按钮的点击永远不会被脚本覆盖
- 深色下面板单独用 `DARK_PANEL_VARS`（8 个变量），不是用户可选主题，只为面板可读

**顺带删掉的东西**（净减 3,454 字节，−8%）

| 删除项 | 原因 |
|---|---|
| 夜间配色预设 + `canvasFilter` 字段 | 夜间没了 |
| `invert(1) hue-rotate(180deg)` canvas 反相 | 只为夜间存在 |
| 深色图标反相规则（`data-wrbg-dark`） | 同上 |
| `syncNativeTheme()` | 不再抢状态 |
| `followSiteDark()` + `lastLightId`（v1.4.2 刚加的映射） | 不需要映射了 —— 站点按钮自己就是正主 |
| 「隐藏站点深色按钮」开关 + `hideDark` 状态 + `data-wrbg-hidedark` + `.wrbg-toggle` CSS | 不再需要隐藏它 |

> 教训：v1.4.0→v1.4.2 花了三轮去「让两个控制器共存」（先是藏起来，再是主从映射），
> 正确的做法其实是**砍掉一个**。这就是 ponytail 说的「最好的代码是你没写的代码」。

### v1.4.2 · 让站点原生「深色」按钮真正生效（问题甲的另一种解）

**背景**：v1.4.0 的 D4 加了个「隐藏站点深色按钮」开关，但打开开关露出那个按钮后，
**点它没有任何反应** —— 因为一个状态只能有一个主人：脚本已经接管了明暗，
站点按钮只是切换 `body.wr_whiteTheme`，会被 `syncNativeTheme()` 立刻改回去，
而且脚本的 CSS 全部带 `!important`，站点的深色样式也压不过。

**修法**：不再让两边争抢，改成**主从映射**。body class 观察器里分两种情况：

| 开关状态 | 行为 |
|---|---|
| 开着（原生按钮被隐藏） | **脚本主导**：任何外部改动都拉回本脚本的配色（原行为） |
| 关掉（原生按钮可见） | **站点主导**：跟随按钮在本脚本配色里切夜间 ↔ 上一次的浅色 |

判定「是不是用户点的」不需要额外监听 —— 按钮被隐藏时，类变化不可能是用户点击造成的，
所以直接用 `state.hideDark` 就能干净地区分两种情况，不会误判、也不会自激
（脚本自己的写入仍由 `mutatingBodyClass` 标记屏蔽）。

用 `lastLightId` 记住上一次的浅色配色，所以「米黄 → 夜间 → 再点 → 米黄」能回到原来那套，
而不是永远落到白色。`apply()` 里顺手维护这个变量（浅色配色时更新），不需要额外的赋值点。

**验证**（回归新增 3 条断言，全绿）

```
PASS  跟随站点深色按钮：点深色 → 切到夜间     [colorId=night]
PASS  跟随站点深色按钮：再点 → 回到原浅色     [colorId=sepia]
PASS  开关打开时脚本主导：外部改动被拉回      [colorId=sepia 有whiteTheme=true]
```

### v1.4.1 · 按 ponytail（YAGNI）自我审查后瘦身

**起因**：用户指出「打开开关露出站点原生深色按钮，但那个按钮没作用」——
这是一次典型的过度工程：**我把一个硬编码常量「升级」成了 UI 开关，却没有先验证它暴露出来的东西能不能用。**
加了表面积，制造了新问题。

**审查结论（用 ponytail 的尺子量自己）**

| 我做的东西 | 判决 |
|---|---|
| D4「隐藏深色按钮」开关 | ❌ 反例：暴露了不能用的按钮（详见下方"一个状态只能有一个主人"） |
| F11 检测 | ⚠️ 实现过深 → 本轮砍掉 |
| 主题失效自检 | ⚠️ 实现过深 → 本轮砍掉 |
| 亮度百分比 / 面板钳制 / 快捷键 | ✅ 各几行，保留 |

**本轮实际改动（只删实现深度，行为完全不变）**

| 项 | 改前 | 改后 |
|---|---|---|
| 自检的颜色比较 | `rgbTuple()` 正则解析 + 三通道 ±3 容差 | 直接比字符串（`--wrbg-bg` 与计算值去掉空格后必然相等），删掉整个解析函数 |
| 自检的防误报 | 700ms + 700ms **两阶段确认** + 签名去重 | 单次 1200ms（远晚于站点 .2s 的过渡）+ 一个布尔标记 |
| F11 的基线学习 | 独立函数 `sampleChromeHeight()` + 初始化时额外调用一次 | 折进 `syncFullscreenUI()`（它本来就在初始化/resize/fullscreenchange 都会跑），少一个函数、少一次调用 |
| F11 的判定 | 三层嵌套 + `try/catch` | 三条判断压成三行，去掉 `try/catch`（这些属性不会抛） |
| **死代码** | `isFullscreen()` 定义后从未被调用 | **删除** |

**净减 852 字节（约 37 行）。没有砍到一半**，因为剩下的都是"载荷"：
- F11 的两条互补信号（有基线 / 无基线）各自覆盖不同系统，砍掉任一条都会丢功能
- 自检的「未找到任何预期容器」分支是站点彻底改版时的唯一信号
- 用户要求「完全不影响功能」，所以到此为止

**过程中回归脚本抓到一个真回归**（这正是它存在的意义）：
把基线采样全挪进 resize 监听后，初始化到首次 resize 之间 `chromeH=0`，
于是走了「无基线」分支（`innerHeight > availHeight`）—— 真实浏览器里成立，
但 headless 没有真实屏幕，`availHeight` 可能小于 `innerHeight` → **误判成全屏**。
修法比原来更省：把学基线塞进 `syncFullscreenUI` 本身。

**另一个发现：行数指标不可信。** 同一次测量 `Get-Content` 数出 923 行、
按 `\n` 切分数出 996 行。所以本项目的瘦身以**字节数**为准。

### v1.4.0 · 四项可用性改进（D 类）

| 项 | 改前 | 改后 |
|---|---|---|
| **快捷键** | 只有 `Esc` 关面板 | 加 `b` 开/关面板。**不带修饰键 + 不在输入框/编辑区里**才响应（`isContentEditable` / `INPUT` / `TEXTAREA` / `SELECT` 一律忽略），并用捕获阶段 + `stopPropagation` 吃掉事件，避免和站点冲突 |
| **窄窗口定位** | 面板用 `right:96px` 绝对定位，极窄时可能超出视口 | 打开时 `clampPanel()` 测量矩形，用 **`transform` 平移**推回视口内（留 12px 边距） |
| **亮度** | 只有滑杆 | 右侧加百分比读数（`tabular-nums` 等宽数字），便于把同一套设置复现到别的机器 |
| **原生深色按钮** | `HIDE_NATIVE_DARK_BUTTON` 是硬编码常量，想改必须编辑脚本源码 | 变成面板里的开关，存进 `settings.hideDark`。实现上把 CSS 规则改成由 `html[data-wrbg-hidedark]` 属性控制，标记恒打、显隐由属性决定 |

**为什么钳制必须用 `transform`**：面板的包含块是工具条而不是视口，
直接改 `left/right` 会以工具条为基准，算出来的位置是错的；
`transform` 永远以元素自身当前位置为基准，与包含块无关，所以才能准确平移。

**验证**：回归脚本新增 4 组断言，全绿。横向快照里
「面板完全落在视口内」的结果是 `x=12` —— 正是钳制把面板从视口外推回来的痕迹。
断言总数从 20 项增至 **28 项 × 2 个模式快照 = 56 项**。

### v1.3.0 · 三项健壮性改进

**A. 修掉「从非阅读页进入阅读页时脚本不激活」**

原代码 `boot()` 第一行就是 `if (!isReaderPage()) return;` —— 而 `boot()` 只在页面加载时执行一次。
于是从首页/书架用**前端路由**切进阅读页（页面不刷新）时，观察器根本没装上，
也就没人会发现 URL 变了，**脚本一行都不执行**，必须手动 F5。

修复：拆成两段 —— `install()` 无条件安装观察器/监听器（不再按页面类型早退），
`activate()` 负责阅读页初始化且可重复调用；`onMaybeNavigate()` 与新增的 `popstate`
监听都会在进入阅读页时触发 `activate()`。

**B. 回归验证固化为 `工具/回归验证.ps1`**

把「SingleFile 快照 + 注入脚本 + headless 渲染 + 断言」这套流程做成一条命令：

```powershell
pwsh -File "工具\回归验证.ps1"      # 自动发现快照；退出码 0 = 全通过
```

覆盖 **20 项断言 × 2 个模式快照**：按钮注入、面板开关、遮罩是否盖满视口、
浅色/夜间各容器底色是否等于 `--wrbg-bg`、canvas 滤镜是否符合预期、
全屏按钮三态（普通 / 模拟 F11 / 不误触 API 全屏）、控制台是否报错。

脚本里写死了两个 **headless 已知陷阱**，避免重复踩：
① 虚拟时间会冻结 `transition` 动画时钟 → 必须先注入 `*{transition:none}`，
否则计算的 `background-color` 停在中间值；② 快照是保存时窗口宽度下的静态 DOM，
窄窗口布局与真机不同 → 断言只做计算样式/几何，不做像素比对。

**C. 主题失效自检**

站点改版的主要危害是**静默失效**：按钮还在、控制台不报错，但颜色就是不生效。
现在 `apply()` 后抽查「本应被覆盖的元素」（`body` / `.app_content` /
`.readerChapterContent` / `.wr_horizontalReader_app_content`）与 `--wrbg-bg` 比对；
**间隔 700ms 连测两次**（避开 `background-color` 过渡的中间值），两次都不符才告警一次：

```
[wrbg] 主题未生效，微信读书可能改版了。
  预期底色：rgb(246,239,221)
  实际不符：.readerChapterContent 实际 rgb(255, 255, 255)
  请运行「工具/weread-probe2.js」，把报告反馈给脚本作者。
```

若阅读页已渲染（工具条在）却一个预期容器都找不到，也会提示「选择器可能已失效」。

**没有待办项。** 所有功能均已在真机确认。

### v1.1.3 · 双栏模式工具条背后的深色竖条 —— 真因是**遮罩**，不是阴影

> ⚠️ 这条我一开始判断错了：v1.1.2 我把原因归给了按钮投影并改掉了 box-shadow。
> 那是错的，v1.1.2 的改动已在 v1.1.3 **回滚**。真正的原因如下。

**现象**：双栏模式下、**面板打开时**，工具条背后出现一条贯穿整列的深色竖条；
用站点自带的按钮（如字号面板）时没有，纵向滚动模式也没有。

**根因**：`.readerControls` 在双栏模式下带 `transform: translateY(-50%)`
（纵向模式没有 transform）。而 **transform 会让元素成为 `position: fixed` 后代的包含块** ——
于是我的遮罩 `.wrbg-mask{position:fixed; inset:0}` 不再是「整个视口」，
而是被缩成了「工具条那一格」：

```
横向/双栏   .readerControls rect x1266 y134 w48 h504   transform=matrix(1,0,0,1,0,-252)
            .wrbg-mask      rect x1266 y134 w48 h504   ← 48×504 的深色矩形 = 那条竖条
纵向单栏    .readerControls rect x1226 y278 w48 h432   transform=none
            .wrbg-mask      rect x0 y0 w1355 h758      ← 正确覆盖视口
```

这一条同时解释了用户观察到的三个特征：**只有面板打开时才有**（遮罩只在打开时显示）、
**站点自带按钮没有**（站点弹层不带这个遮罩）、**只有双栏模式有**（纵向工具条无 transform）。

**修复**：不用 `inset:0`，改用负的 `100vw / 100vh` 向外扩，
这样无论包含块是谁都能盖满整个视口。

```css
.wrbg-mask {
  position: fixed;
  top: -100vh; left: -100vw; right: -100vw; bottom: -100vh;
  z-index: 109;
}
```

**验证**（遮罩矩形 + 像素采样双重确认）

| 模式 | 遮罩 rect（修复后） | 是否盖满视口 |
|---|---|---|
| 横向 / 双栏 | `x-104 y-624 w2788 h2020` | true ✅ |
| 纵向单栏 | `x-1370 y-758 w4095 h2274` | true ✅ |

像素采样（面板打开时，整页应统一压暗 7%）：

| 采样点 | 修复后 |
|---|---|
| 工具条左侧 10px | `207,216,200` |
| 远离工具条 | `207,216,200` | 
| 页面左上角 | `207,216,200` |

三点完全一致 → 竖条消失（修复前工具条那一格是 `x1266 y134 w48 h504` 的独立暗块）。

**真机确认（已闭环）**

- ✅ 纵向 + 夜间**上下滚动**时，滚过去/滚回来的区域文字始终清晰。
  纵向阅读里滚过的区域由 canvas 位图呈现，脚本对该位图做了反相
  （`invert(1) hue-rotate(180deg)`；已确认位图是**透明字形层**，所以反相是正确的做法），
  真机实测通过。

**维护用工具**（`工具/` 目录，站点改版时用来重新定位 DOM）

- `weread-probe.js` —— 环境与引擎状态普查
- `weread-probe2.js` —— 判定正文是 DOM 还是 canvas、背景画在哪个元素上
- `安全审查.ps1` —— **每版发布前必跑的封号风险审查**（见第九节）
- `回归验证.ps1` —— **每版改动后必跑的功能回归**（20 断言 × 2 模式，见 5.B）

---

## 六、工程约定

- **单一 state → `apply()` 幂等重放**，避免状态漂移
- **抗重渲染**：`MutationObserver` + `requestAnimationFrame` 节流，Vue 重渲染后自动补回按钮；插入失败有 `appendChild` 兜底
- **样式优先级**：`html[data-wrbg-*]` 前缀 + `!important`，稳定压过站点的 `.wr_whiteTheme` 规则
- **明暗同步**：站点 CSS 以「无 `wr_whiteTheme`」为深色基底，所以选浅色配色时补上它、选夜间时移除它，让脚本没覆盖到的弹层也跟着变
- **纯 DOM/CSS，不改网络层**，降低风控风险
- **可调试**：`window.wrbg.get/set/reset`

---

## 七、已评估但明确不做

### 7.1 排版功能（边距 / 行距 / 段间距 / 首行缩进）

引擎无参数、CSS 变量只写不读、排版结果烘焙进 transform 且只保留一屏 DOM。
唯一出路是 hook 引擎绘制调用 —— 已决策不走。详见第三节。

### 7.2 在单栏模式下加「书友想法」

**需求**：站点只在双栏模式下提供「书友想法」，希望在单栏模式下也有。

**实测**：单栏模式的 DOM 里**根本没有这个功能**，不是被隐藏：

| 标记 | 纵向单栏 | 横向双栏 |
|---|---|---|
| `showBookReviews`（按钮） | 0 | 10 |
| `reviews_panel`（面板） | 0 | 12 |

**结论：不做。** 原因是风险不对称，而不是技术不可行：

- 要自己实现，就必须**用用户的 cookie 去调站内私有接口**（endpoint / 参数 / 签名都要逆向）。
  而微信读书的整条前端架构（canvas 光栅化、逐字绝对定位、随机 hash 类名）**就是为反爬建的**，
  脚本化调用接口正好落在它的检测目标上。
- 行为特征异常：由滚动/定时触发的请求，缺少对应的 UI 交互，频率可能高于正常用户。
- 一旦触发处罚，微信读书账号挂在微信体系上，波及面比一个只读面板的收益大得多。

**替代方案**：想看书友想法时切换到双栏模式即可（站点自带，零风险）。

### 7.3 参考第三方脚本 Daotin/WeRead-BGChanger 后的结论：不借鉴

**结论：不借鉴，保持现状。** 全屏阅读原本考虑过，实测不好用，直接用 F11 代替。

**它有价值的地方**

- ✅ **独立交叉验证**：它的 `getTargetElement()` 在双栏模式下返回 `.readerChapterContent`
  —— 正是我们 v1.1.1 排查很久才定位到的「白卡」。两条独立路径撞到同一结论，
  反证那个修复方向是对的。
- 面板挂 `document.body`、用按钮矩形反算位置，结构上天然绕开
  `transform` 成为 `position:fixed` 包含块的问题（我们是用负 `100vw/100vh` 打的补丁）。

**为什么不借鉴**

| 项 | 原因 |
|---|---|
| 全屏阅读 | **不借鉴它的做法**：它对**内容元素**调 `requestFullscreen()`，要处理 `:fullscreen` 下的 `overflow` 特判、手动隐藏顶栏、还需 `setInterval(fixFooter, 100)` 每 100ms 纠正底栏。我们改用全屏 `document.documentElement` 自己实现（见第八节）—— 同样一行 API，零 hack |
| 只改单个元素的内联样式 | 顶栏、底栏、悬浮按钮、弹层都不覆盖，且内联样式会被 `!important` 规则压过 |
| 面板定位 / 无遮罩 | 是减少代码量的写法，不优于现有已验证方案，不值得为此重构 |
| 背景图库 | 内置 476KB base64，且走 `@require` 远程加载 |

**⚠️ 记录该脚本的三个坑（供以后参考同类脚本时警惕）**

1. **`@require` 远程依赖**：它 `@require` 了一个 greasyfork 上的 476KB 脚本，
   等于每次加载都执行第三方 URL 的代码 —— 那个地址内容一旦变动就是任意代码执行，
   且用户无感知。它顶层还直接 `window.backgroundImages.map(...)` 不做存在性判断，
   地址一挂整个脚本崩。**我们的脚本坚持零外部依赖。**
2. **Observer 泄漏**：两个 `MutationObserver` 在 `init()` 内创建且从不 `disconnect()`，
   而 `init()` 会被外层 observer 反复调用 → observer 累积。
3. **两个脚本不能共存**：它靠读站点「深色」按钮的类名判断明暗，而我们会接管
   `wr_whiteTheme` 并隐藏该按钮；同时安装会两个按钮两个面板，且背景改动上我们的
   `!important` 会压过它的内联样式，它的功能基本失效。**二选一。**

### 附：当前脚本的风险评估

结论：**没有实质封号风险。**

审计证据（`weread-bg-theme.user.js`）：

| 检查项 | 结果 |
|---|---|
| `fetch` / `XMLHttpRequest` / `WebSocket` / `sendBeacon` | 0 |
| `GM_xmlhttpRequest` / `GM_setValue` / `@connect` / `@require` | 0 |
| `eval` / `new Function` | 0 |
| 脚本内出现 `http(s)://` | 仅 `@namespace` / `@match` 元数据 + 一个 SVG 命名空间字符串 |

脚本实际只做五件事：注入一个 `<style>`、往 `<html>` 写 `--wrbg-*` CSS 变量、
在既有工具条里插两个按钮和面板、切换 `body` 的 `wr_whiteTheme` 类（本地显示状态）、
写一个 `localStorage` 键。不改请求、不绕过付费、不抓取正文。
`requestFullscreen()` 属于浏览器原生 API，不产生任何网络请求。

---

## 八、全屏阅读按钮（v1.2.0）

**需求**：想要一个按钮实现 F11 的效果，省得每次按键盘。

**先说清一个限制**：**JS 无法触发浏览器级的 F11。** F11 是全屏整个浏览器窗口，
属于浏览器自身功能，网页脚本没有这个权限，任何脚本都做不到。

**但 `document.documentElement.requestFullscreen()` 在视觉上等效**：

| | F11 | `documentElement.requestFullscreen()` |
|---|---|---|
| 浏览器 UI | 隐藏 | 隐藏 |
| 页面铺满屏幕 | 是 | 是 |
| 站点布局 | 不受影响（只是视口变大） | 不受影响（同理） |
| 退出 | F11 / Esc | Esc / 再点按钮 |
| 触发者 | 只能用户按 | 脚本可调（需用户手势，点击天然满足） |

**关键点：全屏的对象必须是 `<html>`，不能是内容元素。**
这正是第三方脚本那个全屏难用的原因 —— 它全屏 `.app_content`，于是该元素被迫撑成视口大小，
必须额外处理 `:fullscreen` 下的 `overflow`、手动隐藏顶栏，还要 `setInterval(fixFooter, 100)`
每 100ms 轮询纠正底栏。全屏 `<html>` 则一行 API 搞定，零 hack：视口变大后排版引擎
自己重排，与按 F11 的行为完全一致。

**实现要点**

- 按钮：工具条 ☀ 下方新增 ⛶ 按钮，`.readerControls_item` 同款样式 + 悬停提示
- 图标随状态切换：未全屏「展开」四角，已全屏「收拢」四角，提示文案同步为「退出全屏」
- 监听 `fullscreenchange` / `webkitfullscreenchange` 同步图标（按 Esc 退出也能同步）
- 不支持 Fullscreen API 时安静降级，不抛错
- 两个按钮的排布：`.wrbg-wrap` 改为 `display:flex; flex-direction:column; gap:24px`，
  与站点 `.readerControls>*{margin-bottom:24px}` 的间距一致（实测两按钮间距 72px = 48+24）

**验证**（纵向 + 双栏两模式，headless 渲染）

```
按钮数=2  全屏按钮=1   wrap display=flex gap=24px
全屏按钮 48×48  title=全屏阅读  图标含 expand 路径 = true
背景按钮 y566 → 全屏按钮 y638   （间距 72px，与站点一致）
面板 open=1 rect 440×325
errors: (none)
```

### 8.1 F11 状态的识别与「退不出」的硬限制（v1.2.1）

**用户反馈**：用 F11 进全屏后，脚本按钮仍显示「全屏阅读」，点它退不出，反而又叠了一层脚本全屏。

**两个硬限制（浏览器安全边界，任何脚本都突破不了）**

1. **JS 无法退出浏览器原生全屏（F11）** —— 没有对应 API。所以按钮在 F11 状态下
   **不可能**真的帮他退出，只能提示。
2. **F11 没有事件、也没有 API 可查询** —— 既没有 `fullscreenchange`，也没法直接问
   「窗口是否全屏」。只能用视口尺寸**推断**。

**推断方案（三条信号，交叉判断以免误报）**

```
① 视口高度几乎占满屏幕高度        innerHeight >= screen.height - 40
② 浏览器 UI 的高度相对基线塌掉      (outerHeight - innerHeight) < chromeH * 0.5
③ 没有基线时的兜底：视口盖过任务栏   innerHeight > screen.availHeight + 2
```

- `chromeH` = 非全屏时 `outerHeight - innerHeight`（标签栏+地址栏的高度），
  在页面加载和每次 resize 时采样，且**只在 > 4px 时更新**，所以全屏时不会把 0 记进去
- ② 是主判据；③ 用于「页面本身就在 F11 下加载、没来得及采样基线」的情况
- 触发同步的时机：`fullscreenchange`（API 全屏）+ `resize`（F11 只能靠它，150ms 防抖）

**按钮行为**

| 当前状态 | 按钮显示 | 点击行为 |
|---|---|---|
| 非全屏 | ⛶ 全屏阅读 | 调 `requestFullscreen()` 进入 |
| 脚本自己开的全屏（API） | ⛶ 退出全屏 | 调 `exitFullscreen()` 退出 ✅ |
| F11 原生全屏 | ⛶ 退出全屏（按 F11） | **退不了**，弹提示「浏览器全屏请按 F11 或 Esc 退出」；不会误触 API 全屏 |

**验证**（伪造视口参数模拟 F11，7 个状态全通过）

```
A 正常窗口   label=全屏阅读           icon=展开    native=false
B 模拟F11    label=退出全屏（按 F11）  icon=收拢    native=true      ← 识别成功
C F11下点击  requestFullscreen被调用=false  提示="浏览器全屏请按 F11 或 Esc 退出"
D 退出F11    label=全屏阅读           icon=展开    native=false     ← 状态回落
E API全屏    label=退出全屏           icon=收拢    api=true
F 点击退出   exitFullscreen被调用=true
G 回到普通   label=全屏阅读           icon=展开
errors: (none)
```

另暴露诊断接口，方便真机排查推断是否准确：

```js
wrbg.fullscreen
// { api, native, chromeH, innerHeight, screenHeight, availHeight, outerHeight }
```

---

## 九、交付检查制度（每版必做）

**要求**：**每一个版本交付前，都必须通过交付检查。** 不通过不得交付。

不靠"记得审"，而是做成**一个入口、五道关卡、自动留档**：

```powershell
pwsh -File "工具\交付检查.ps1"
# 退出码 0 = 全部通过；>0 = 未通过的关卡数
```

| # | 关卡 | 做什么 | 为什么需要 |
|---|---|---|---|
| 1 | **封号风险审查** `安全审查.ps1` | 五类危险模式必须命中 0（见下） | 防止不小心引入可被服务端观测的行为 |
| 2 | **功能回归** `回归验证.ps1` | 43 项断言 × 2 个模式快照 | 改了 A 别弄坏 B（本轮就抓到过真回归）|
| 3 | **死代码检查**（内联） | 函数名全文只出现 1 次 = 只有定义没有调用 | 写完忘了接上；本轮靠它抓到 `isFullscreen()` |
| 4 | **体积** | 与上次记录对比并报增量 | 悄悄膨胀；本项目**以字节数为准**（行数指标不可信）|
| 5 | **像素验证** `像素验证.ps1` | 15 种组合实拍 1240×2772，页边三段色值对官方实测，另验云的走向 / 月亮位置 / 色调方向 | **v1.8.0 的教训：计算样式全绿 ≠ 观感没问题。** 只断言计算样式等于没验「长什么样」 |

每次运行都会往 **`工具/审查记录.md`** 追加一行：版本 / 时间 / 字节 / 回归 / 像素验证 / 审查 / 死代码。
所以「这一版审过没有」有据可查，不依赖记忆。

> 第 5 关是 v2.0.0 补上的，起因就是 v1.8.0 那次"计算样式 80 项全绿、实际观感却很差"。
> **教训：验证手段的盲区，就是缺陷的藏身处。** 断言什么，就只能保证什么。

加这一关时又踩到两个 PowerShell 5.1 的坑，都写进脚本注释了：
1. 往页面注入脚本串**必须包 `<script>` 标签**，否则整段被当文本（20 格全返回同一个值）
2. `@(Lum $a, Lum $b, Lum $c)` 会被当成**一条命令带三个参数** → 空数组 → 伪造出一堆假 FAIL

另外三个 `.ps1` 全部加了 **UTF-8 BOM** —— 本机宿主是 Windows PowerShell 5.1 + ANSI 代码页 936，
无 BOM 的 UTF-8 中文注释会被当 GBK 解码，脚本直接语法错（v1.8.0 那次"交付检查自己崩了"就是这个）。

```
| 版本 | 时间 | 字节 | 功能回归 | 封号审查 | 死代码 |
|---|---|---|---|---|---|
| v1.5.0 | 2026-09-24 21:52 | 39,125 | 60 通过 / 0 失败 | 通过 | 通过 |
```

### 审查原理

封号打击的是**内容获取**和**账号行为**，不是本地改样式。所以只须确认脚本
不碰下列任何一类即可 —— 每类都要求命中 **0**：

| 类别 | 检查的模式 | 为什么危险 |
|---|---|---|
| 网络请求 | `fetch` `XMLHttpRequest` `WebSocket` `EventSource` `sendBeacon` `GM_xmlhttpRequest` | 产生可被服务端观测的异常流量 |
| 动态执行代码 | `eval` `new Function` `setTimeout("…")` `setInterval("…")` | 隐藏行为、绕过审查 |
| 外部依赖/注入脚本 | `@connect` `@require` `@resource` `unsafeWindow` `<script` | 供应链风险（第三方 URL 内容可变） |
| 读取/篡改站点数据 | `document.cookie` `sessionStorage` `indexedDB` `__vue__` `.prototype` | 越界访问账号/内部状态 |
| 模拟用户操作 | `.click()` `dispatchEvent` `MouseEvent` `KeyboardEvent` `.submit()` `.focus()` | 自动化行为特征，风控第一抓手 |

另有两项**命名空间**检查：

- `@grant` 应为 `none`（不申请 GM_* 权限）
- `localStorage` 键必须以 `wrbg.` 开头（不覆盖站点数据）

### 审查记录

| 版本 | 方式 | 结果 |
|---|---|---|
| v1.5.0 | `工具/交付检查.ps1`（全套） | ✅ 通过 |
| v1.4.2 | `工具/安全审查.ps1` | ✅ 通过（五类全 0）|
| v1.4.1 | `工具/安全审查.ps1` | ✅ 通过（五类全 0）|
| v1.4.0 | `工具/安全审查.ps1` | ✅ 通过（五类全 0）|
| v1.3.0 | `工具/安全审查.ps1` | ✅ 通过（五类全 0）|
| v1.2.1 | `工具/安全审查.ps1` | ✅ 通过（五类全 0）|
| v1.2.0 及更早 | 人工取证审查（同类检查项） | ✅ 通过（全 0）|

**制度**：改完跑 `工具/交付检查.ps1`，四道关卡全过才交付，并把结果贴给用户。
v1.5.0 起记录自动写入 `工具/审查记录.md`。

无网络请求、无动态执行、无外部依赖、无站点数据读写、无模拟操作。
全部行为限于本地 CSS/DOM 显示层，服务端收到的请求与不装脚本时完全一致。

### 已知的残余风险（不因审查通过而消失）

1. **用户协议层面**：多数网站 ToS 都有「不得修改客户端/使用第三方工具」的兜底条款，
   严格按字面，任何油猴脚本都算触碰。但实践中这类条款用于打击抓取/破解/批量操作，
   不用于处罚改配色的用户。
2. **可检测性**：技术上站点能检测到 DOM 里多了 `data-wrbg` 属性和 `window.wrbg`。
   属于「可能但无先例」的风险。
3. 最大可能是**将来脚本失效**（站点加 DOM 校验），而非封号。

### 风险边界（对比才看得清）

| 行为 | 风险 |
|---|---|
| 本脚本：本地 CSS/DOM 显示层 | ✅ 无 |
| 走接口拿「书友想法」数据 | ❌ 脚本化调私有 API，正落在反爬检测目标上 |
| hook 引擎绘制调用改排版 | ❌ 侵入式改写站点逻辑 |
| 抓取/导出正文 | ❌ 直接针对反爬体系 |
| 批量刷时长 / 自动翻页 | ❌ **这才是真正会被封的行为** |

后四项均已评估并**明确放弃**（见第七节），正是出于这个考量。
