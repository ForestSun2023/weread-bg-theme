# ============================================================================
# 微信读书网页版 · 背景颜色主题 —— 仓库忽略规则
# ----------------------------------------------------------------------------
# 这个仓库只发布三样东西：脚本本体、文档、验证工具。
# **不发布任何测试素材。** 下面每一类都写了为什么，改动前请先读一遍。
# ============================================================================

# ---------- 登录凭据：绝对不能提交 ----------
# HAR 是浏览器抓包的原始记录，内含 Cookie（等于你的登录态）。
# 本项目实测一份 HAR 里有 20 处 Cookie —— 一旦推上公开仓库等于把账号交出去。
*.har
*.cookies.txt

# ---------- 版权内容：不是我们的东西，不发布 ----------
# ① SingleFile 保存的阅读页快照（约 19 MB）。
#    验证工具要用它，但里面是**整章小说正文** + 站点完整 DOM，属于版权内容。
#    它只应留在本机 —— 见 README「开发与验证 · 准备测试夹具」。
/*.html
/*.htm
# ② 手机版官方 App 的 40 张截图：当初用来逐像素采样配色，属于腾讯的内容。
官方背景截图/
# ③ 官方 App 图标。
微信读书-iOS-*
# ④ 官方安卓安装包（约 136 MB）。当初为确认「背景不是位图」才解包，不必随仓库分发。
base.apk*
_apk/

# ---------- 本机临时文件 / 顺手下载的东西 ----------
_cgi-bin_*
微信图片_*.jpg

# ---------- 编辑器 / 操作系统 ----------
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~
~$*
*.tmp
*.log
